// Форматы возвращаемых обьектов

const createUserInfo = () => ({
    userName: null,
    userID: null,
    dateCreate: null,
    countProjects: 0,
    countTasks: 0,
});

const toReturn = {
    createUserInfo,
};

export default toReturn;