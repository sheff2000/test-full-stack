import express from 'express';

const router = express.Router();

//router.get('/projects', userController.login);
//router.put('/updatepassword/:userId', verifyToken, updateUserPassword);

export default router;